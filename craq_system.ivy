#lang ivy1.7

include spec

isolate system = {
    object server(me: node) = {
	function dBitMap(K: key) : bool
	instance mvMap : ordered_map(key_tups, value)
	
	after init {	    
	    dBitMap(K) := false
	}
    }    
} with spec, node, key_tups
